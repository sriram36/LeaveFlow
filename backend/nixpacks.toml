[phases.setup]
nixPkgs = ["python312", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -c 'from app.database import init_db; import asyncio; asyncio.run(init_db())'"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
